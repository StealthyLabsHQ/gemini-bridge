---
description: Query Gemini Lite (fast, economical). Usage: /gemini:lite <prompt>
---

Call the MCP tool `query_gemini` with `tier="lite"` and `prompt="$ARGUMENTS"`.

Display the result to the user. If no arguments are provided, ask the user what they want to ask Gemini.
