---
description: Query Gemini Flash (balanced, speed/quality). Usage: /gemini:flash <prompt>
---

Call the MCP tool `query_gemini` with `tier="flash"` and `prompt="$ARGUMENTS"`.

Display the result to the user. If no arguments are provided, ask the user what they want to ask Gemini.
