---
description: Query Gemini Pro (max reasoning, 100 req/day). Usage: /gemini:pro <prompt>
---

Call the MCP tool `query_gemini` with `tier="pro"` and `prompt="$ARGUMENTS"`.

Display the result to the user. If no arguments are provided, ask the user what they want to ask Gemini.
